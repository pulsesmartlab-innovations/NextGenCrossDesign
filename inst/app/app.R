# Entry point so RStudio's "Run App" button works on the installed package.
# Open this file in RStudio and click Run App, or from any R session:
#   nextgenCrossWorkbench::run_workbench()
library(nextgenCrossWorkbench)
workbench_app(getwd())
